# Second Brain OS - Backend Server

This is the express server for persistent file database storage and proxying LLM / AI requests safely.

## Scripts
- **Development**: Runs `tsx server.ts` at the workspace root.
- **Production**: Compiles and runs `dist/server.cjs`.
